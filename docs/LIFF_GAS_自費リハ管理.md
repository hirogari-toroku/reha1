# LIFF / GAS / 自費リハ管理

## 主要リポジトリ

- ローカルrepo: `/Users/soichi/Documents/Codex/reha1`
- GitHub repo: `https://github.com/hirogari-toroku/reha1`
- GitHub Pages: `https://hirogari-toroku.github.io/reha1/`
- LIFF / GitHub Pages HTML: `/Users/soichi/Documents/Codex/reha1/index.html`
- GASコード: `/Users/soichi/Documents/Codex/reha1/gas/コード.js`
- GAS script ID: `10RcFcwKK-YbzFnyH67Kg1aVZ1KaNGahg0uwiUVZmemAMQyJnxu3ATIcL`
- 自費リハ管理1スプレッドシートID: `1M992L2LX1T_3DSKBiv-1_7-VLbApJHanjRD36XRfwuc`

## 直近の反映済み作業

- LIFF画面から給与明細ボタンを削除し、スタッフフォルダのみに整理。
- 予定カードの利用者名横にカルテボタンを追加。
- 利用者フォルダボタンはカード内から削除。
- 予定外実績をLIFF画面に表示。開始・終了・変更・キャンセル対象ではなく閲覧中心。
- 利用者画面の予定カードを新しい順に変更。
- 「お疲れ様です」が利用者名「お疲れ」と誤認される問題を対策。
- 回数券管理、給与集計、給与明細PDF作成、振込CSV作成を調整。
- テスト利用者は給与計算対象外、給与0円の明細は作成しない方針に修正。
- 給与明細PDFを各スタッフフォルダへ移動するコマンドを追加。
- GMO振込CSVを `GMO振り込みCSV確定` フォルダへ移動する処理を追加。
- スタッフ登録フォーム、口座登録フォーム、スタッフマスタ連携を調整。
- スタッフマスタで `姓フリガナ` + `名フリガナ` から `受取人名` を自動補完。
- 口座名義カナは半角カナへ変換。
- 既存スタッフフォルダID確認、新規スタッフフォルダ作成・共有の仕組みを追加。
- スタッフ登録フォーム回答後、自動でスタッフマスタ取込・既存フォルダ確認・必要時フォルダ作成/共有する仕組みを追加。
- `appsscript.json` に `executionApi` を追加し、本番Webアプリデプロイ `AKfycbxCE2wBGAhY3vbYT0sGfyVhGYH36OvoxQyWAfR-ullAcqS90FQ2jCkm5G4SrLxyorwwqQ` をバージョン140へ更新。
- 管理者限定Webアプリ action `adminSetupStaffQuestionnaireAutoImportTrigger` を追加。管理者LINE ID確認後、`setupStaffQuestionnaireAutoImportTriggerCore_()` を実行する。
- 2026-09-06、上記action経由でスタッフ登録フォーム自動処理トリガーを再設定済み。結果は、既存トリガー削除1件、新規トリガー作成1件。
- 2026-09-06、ダミースタッフ `ダミー登録者0906` でスタッフ登録後のLIFF動作をリハーサル。予定登録、予定一覧表示、開始実績、終了実績、重要事項リンク取得は成功。
- 上記リハーサルで、完了済み予定に再度開始実績を登録できてしまう問題を確認。GASで予定IDつき実績登録時の状態ガードを追加し、本番Webアプリをバージョン141へ更新。完了済み予定の再開始は重複としてブロックされることを確認済み。
- 続けて `ダミー登録者0906` を `スタッフ利用者マスタ` 上で `テスト利用者` に紐づけ、`LIFF表示用マスタ` を更新。スタッフLIFFの利用者一覧に `テスト利用者` が表示されることを確認済み。
- 予定変更・キャンセルもリハーサル。未開始予定の変更、未開始予定のキャンセル、予定一覧からの非表示は成功。未開始予定への終了登録はブロックされることを確認済み。
- 開始済み/完了済み予定に対する変更・キャンセルを防ぐGASガードを追加し、本番Webアプリをバージョン142へ更新。開始済み予定の変更/キャンセル、完了済み予定の変更/キャンセル、他スタッフによる予定操作がブロックされることを確認済み。
- GitHub Pages側の `index.html` に、開始済み/完了済み予定で変更・キャンセルボタンを無効化する修正を反映済み。公開HTMLでも `scheduleLocked` と変更・キャンセル無効化の配信を確認済み。
- 管理者限定Webアプリ action `adminImportStaffBankQuestionnaire` を追加し、本番Webアプリをバージョン144へ更新。管理者LINE ID確認後、スタッフ銀行登録フォーム回答をスタッフマスタへ取り込む。
- 2026-09-06、ダミースタッフ `ダミー登録者0906` の銀行登録フォーム回答を追加し、上記action経由でスタッフマスタへ取り込み済み。銀行コード `0036`、支店番号 `202`、預金種目 `1`、口座番号 `1234567`、受取人名 `ﾀﾞﾐｰ ﾄｳﾛｸｼｬ`、振込先登録状況 `登録済` を確認済み。再実行時は更新0件で、重複更新されないことを確認済み。
- 管理者限定Webアプリ action `adminCreateAndShareStaffFolder` を追加し、本番Webアプリをバージョン145へ更新。管理者LINE IDとスタッフ名を指定して、対象スタッフだけスタッフフォルダID確認・新規作成・Gmail共有を実行する。
- 2026-09-06、`ダミー登録者0906` で対象スタッフ限定フォルダ作成を実行。Driveフォルダ `ダミー 登録者0906 理学療法士` が作成され、スタッフマスタの `スタッフフォルダID` とLIFF表示用マスタのスタッフフォルダリンクへ反映済み。2回目の実行では新規作成0件で、既存フォルダIDがあるスタッフは重複作成されないことを確認済み。
- 同フォルダのDriveメタデータ確認では、所有者は `contact@reha-hirogari.com`、共有状態は owner のみ。GAS実行時のGmail共有は `操作を実行できません` となったため、実スタッフ運用では共有権限または実在Gmailで追加確認する。
- 対象スタッフ限定のフォルダ作成actionは、既に `スタッフフォルダID` が入っているスタッフでもGmail共有だけ再試行できるように修正済み。通常の一括処理は従来通り、フォルダID空欄のスタッフだけを対象にする。
- `スタッフマスタ` に `スタッフフォルダ共有状況` 列を追加。共有成功時は `共有済：メールアドレス`、Gmail未確認時は `Gmailアドレス未確認`、共有失敗時は `共有失敗：エラー内容` を残す。
- 2026-09-06、`ダミー登録者0906` で共有リトライを実行し、`スタッフフォルダ共有状況` に `共有失敗：操作を実行できません` が残ることを確認済み。これにより、登録完了後もフォルダ共有だけ未完了のスタッフをシート上で判別できる。
- ダミースタッフのLIFF初期表示は、本番Webアプリで `テスト利用者`、スタッフフォルダリンク、同意フォームURLを返すことを確認済み。
- 管理者限定Webアプリ action `adminSetupUserQuestionnaireAutoImportTrigger` を追加し、本番Webアプリをバージョン148へ更新。管理者LINE ID確認後、利用者登録フォーム回答後の自動処理トリガーを設定する。
- 2026-09-06、上記action経由で利用者登録フォーム自動処理トリガーを設定済み。結果は、既存トリガー削除1件、新規トリガー作成1件。
- 利用者登録フォーム回答後、自動で利用者マスタ取込、LINE情報反映、紹介者管理、LIFF表示用マスタ更新、利用者登録処理ログ記録を行う。
- LIFF本番デプロイ済み。
- 2026-09-07、管理者LIFFの担当開始準備パネルを実装し、`reha1` commit `8e85f29 Add assignment start readiness panel` をpush。GitHub Actions `Deploy GAS` 成功確認済み。
- 2026-09-08、管理者LIFFの担当開始準備パネルからスタッフへ銀行登録フォーム案内をLINE送信するボタンを実装し、`reha1` commit `fa31753 Add staff bank guide action` をpush。GitHub Actions `Deploy GAS` 成功確認済み。
- 2026-09-08、本番Webアプリで `adminSendStaffBankRegistrationGuide` action が動いていることを確認。疎通確認は `staffId` なしで行ったため、LINE送信は行っていない。
- 2026-09-08、GitHub認証をmacOS Keychainへ保存。`reha1` 用tokenは `hirogari-toroku/reha1.git` に紐づけて保存済み。通常の `reha1` pushでは毎回token入力しない。

## スタッフフォルダ方針

- スタッフフォルダと給与明細フォルダは同じでよい。
- `給与明細フォルダID` は `スタッフフォルダID` に変更済み。
- 既存スタッフのフォルダは新しく作らない。
- 既存スタッフのフォルダリンクは、スタッフマスタにある既存リンクを使う。
- `スタッフ一覧` と `休止中スタッフ` の両方から既存フォルダを探す。
- Drive上のフォルダ構成は今の運用を維持する。
- Driveフォルダの大量作成・大量共有は実行前に確認する。依頼範囲内の少数・既知対象であれば、管理者限定actionで対象を指定して実行し、結果を報告する。

## スタッフ登録手順

スタッフ初回登録から利用者決定後の運用開始までは、`docs/スタッフ登録手順.md` を正とする。

要点:

- スタッフはLINE公式アカウントを友だち追加し、`4` を送る。
- LINE公式アカウントの応答メッセージからスタッフ登録LIFFへ進む。
- LIFFを開いた時点でLIFF用LINEユーザーIDとLINE表示名を取得する。
- その後、スタッフ登録Googleフォームへ進む。
- フォーム回答後、GASトリガーでスタッフマスタ取込、既存フォルダ確認、必要時のスタッフフォルダ作成・Gmail共有、ログ記録を行う想定。
- 銀行情報は初回登録では扱わず、利用者が割り当てられてから銀行登録フォームを手動で送る。
- 銀行登録フォーム案内は、管理者LIFFの担当開始準備パネルから対象スタッフ1名へ送信できる。
- 管理者LIFFでスタッフと利用者を紐づけた場合は、LIFF表示用マスタを自動更新し、担当開始準備パネルを表示する。
- 管理者LIFFの担当開始準備パネルから、銀行登録が未完了のスタッフへ銀行登録フォーム案内をLINE送信できる。
- 将来的には、初回訪問候補日選択、初回訪問予定登録を順に自動化したい。
- 重要事項説明リンクはLINEリッチメニューとLIFF画面で案内できているため、自動送信はしない。

## 利用者登録・担当開始の方針

- 利用者登録は、公式LINEで友だち追加後に `1` を送ってもらい、利用者登録LIFFへ進む。
- 利用者登録LIFF入口は `https://hirogari-toroku.github.io/reha1/?mode=register`。
- 利用者登録フォームと回答スプレッドシートは、Google Driveの `マイドライブ > 2利用者関連資料` にある。
- フォーム本体は `リハビリ利用申請フォーム`、回答スプレッドシートは `リハビリ利用申請フォーム（回答）`。
- 利用者登録フォーム回答スプレッドシートID: `1y8h9EZG47uYVuoV3K1L_m9-JaDP8GRLQ6ZZ-HpgPxKM`
- 利用者登録フォーム回答シートID: `1702028371`
- スタッフ登録フォーム回答スプレッドシートID: `1sQzA9oHCMH712l8gNuoZxrB29eX_UeQg5zHgy_1_tQw`
- スタッフ登録フォーム回答シートID: `1876133724`
- スタッフ銀行登録フォーム回答スプレッドシートID: `1gPLvHUKwYLlB5yaN3vB_QQSZOeBe41JOXGMahvPg8eg`
- スタッフ銀行登録フォーム回答シートID: `399292105`
- 利用者登録フォーム自動処理トリガーは設定済み。フォーム回答後に利用者マスタ取込、LINE情報反映、紹介者管理、LIFF表示用マスタ更新、利用者登録処理ログ記録を自動実行する。手動メニューは復旧用として残す。
- 利用者登録フォームでは、具体的な初回訪問日ではなく、訪問希望曜日と訪問希望時間帯を入力してもらう。
- フォーム内では、スタッフは平日18時以降または土日の方が比較的調整しやすいことを説明する。
- フォーム内では、訪問エリアや時間帯によって交通費が必要となる場合があることを事前案内する。
- スタッフ候補が決まったら、スタッフの訪問可能日から具体的な初回訪問候補日を作り、利用者または家族に選んでもらう。
- 遠方スタッフが候補になる場合は、この候補日選択のタイミングで交通費の目安を伝え、「追加交通費がかかるなら希望しない」選択肢を残す。
- 重要事項説明は、原則として初回スタッフ訪問時にスタッフが説明動画を見せ、その後に同意フォームへ進んでもらう。

## 重要事項説明まわりの実装状況

- `重要事項説明マスタ` という管理用シートからURLを読む設計を追加。
- 管理するURL列:
  - `重要事項説明書PDF URL`
  - `説明動画URL`
  - `同意フォームURL`
- LIFF画面に重要事項説明パネルを追加。
- 登録済みスタッフ・紐づき済み利用者だけに表示。
- 未登録・未紐づけの人には表示しない。
- 本番GAS Webアプリは `@151` にデプロイ済み。
- GitHub Pages用 `index.html` もpush済み。
- 2026-09-07に `contact@reha-hirogari.com` でclasp再認証済み。GitHub Actions用の `CLASPRC_JSON` も更新済みで、`Deploy GAS` 成功確認済み。
- コミット:
  - `8e85f29 Add assignment start readiness panel`
  - `fa31753 Add staff bank guide action`

## まだ必要な操作

1. スプレッドシートの `自費リハ管理` メニューから `📘 重要事項説明マスタを整える` を実行。
2. 作成された `重要事項説明マスタ` の2行目に、PDF・動画・同意フォームURLを入力。
3. `⚡ LIFF表示用マスタを更新` を実行。
4. LIFF画面で表示確認。

## 注意

- Apps Script API の `scripts.run` は、API Executable追加後も、OAuthクライアントとGAS側Google Cloud Projectの不一致により `403 The caller does not have permission` になることがある。
- Codexからスタッフ登録フォーム自動処理トリガーを再設定する場合は、管理者LINE ID付きでWebアプリ action `adminSetupStaffQuestionnaireAutoImportTrigger` を呼ぶ。
- Codexから利用者登録フォーム自動処理トリガーを再設定する場合は、管理者LINE ID付きでWebアプリ action `adminSetupUserQuestionnaireAutoImportTrigger` を呼ぶ。
- Codexからスタッフ銀行登録フォーム回答を取り込む場合は、管理者LINE ID付きでWebアプリ action `adminImportStaffBankQuestionnaire` を呼ぶ。
- Codexまたは管理者LIFFからスタッフへ銀行登録フォーム案内を送る場合は、管理者LINE ID、`staffId`、必要に応じて `userId` 付きでWebアプリ action `adminSendStaffBankRegistrationGuide` を呼ぶ。送信対象は対象スタッフ1名。
- Codexから特定スタッフのスタッフフォルダID確認・作成・共有を行う場合は、管理者LINE IDと `staffName` 付きでWebアプリ action `adminCreateAndShareStaffFolder` を呼ぶ。既にスタッフフォルダIDがある場合は重複作成しない。
- スタッフフォルダ共有の成否は `スタッフマスタ` の `スタッフフォルダ共有状況` を確認する。`Gmail確認` はGmail形式の確認であり、フォルダ共有完了を意味しない。
- `clasp run updateLiffDisplayMaster -p '[true]'` はこの環境では `NOT_FOUND` または権限エラーで失敗することがあるため、メニューまたは管理者LIFF/Webアプリ経由が安全。
- `clasp push` と本番デプロイは成功済み。
- `main` に `gas/**` の変更をpushすると、GitHub ActionsからGAS本番デプロイまで自動で進む。
- GitHub push の認証はmacOS Keychainへ保存済み。token期限切れやrepo権限不足のときだけ、`hirogari-toroku/reha1` 用tokenをGitHub画面でコピーして保存し直す。
- 利用者登録フォーム自動処理トリガーは、2026-09-06にWebアプリ管理者actionから設定済み。

## LIFFモード

GitHub Pagesの `index.html` は、URLパラメータ `mode` で画面を切り替える。

- `?mode=staff`: スタッフ用画面。担当利用者、訪問予定、開始・終了、予定変更、キャンセル、スタッフフォルダを表示する。
- `?mode=user`: 利用者用画面。本人に紐づく予定と予定外実績を確認する。
- `?mode=register`: 新規利用者登録LIFF。LINE情報を保存し、利用申請フォームへ進む。
- `?mode=staff-register`: スタッフ登録LIFF。LINE情報を保存し、スタッフ登録フォームへ進む。
- `?mode=important`: 重要事項説明LIFF。登録済みスタッフまたは紐づき済み利用者だけ確認できる。
- `?mode=admin`: 管理者LIFF。データ整備、紐づけ確認、代理表示テスト、スタッフ利用者マスタの紐づけ保存・解除、銀行登録フォーム案内送信を行う。
- `?mode=qr`: 公式LINE友だち追加用QR表示。

## 管理者LIFF

管理者LIFFは `管理者マスタ` の `LIFF用LINEユーザーID`、またはコード内の固定管理者IDで利用可否を判定する。

できること:

- 未反映のLINEユーザー候補、紐づけ件数、要確認件数、未紐づけ利用者を確認する。
- `一括実行`、`利用者アンケート取込`、`LINE情報を反映`、`ID列を整える` を画面から実行する。
- スタッフまたは利用者の代理表示を読み込む。代理表示は表示確認専用で、開始・終了・変更・キャンセルは実行しない。
- `スタッフ利用者マスタ` の紐づけを追加・更新・解除する。保存・解除後はLIFF表示用マスタも更新される。
- 担当開始準備パネルから、銀行登録フォーム案内を対象スタッフ1名へLINE送信する。

注意:

- 管理者LIFFからの一括実行、紐づけ保存、紐づけ解除はスプレッドシートを書き換える。
- 実行前に、対象スタッフ、対象利用者、実行目的、LIFF表示への影響を確認する。
- 管理者未登録表示が出る場合は、表示されたLIFF用LINEユーザーIDを `管理者マスタ` に登録する。

## データ整備・紹介者管理

`✅ データ整備を一括実行` は、次の処理をまとめて実行する。

- スタッフID、利用者ID、紹介者ID、スタッフ利用者マスタID列の整備
- スタッフ登録フォーム回答のスタッフマスタ取込
- 利用者アンケート回答の利用者マスタ取込
- `LINEユーザー一覧` の紐づけ候補更新
- LINE情報のスタッフマスタ・利用者マスタ反映
- `紹介者マスタ` と `紹介管理` の作成・更新
- 振込先登録状況の更新
- LIFF表示用マスタの更新

紹介者管理の実装:

- `紹介者マスタ`: `紹介者ID`、紹介者名、所属、職種、LINE情報、紹介料対象、紹介料支払状況、状態、メモを管理する。
- `紹介管理`: 紹介日、紹介者、利用者、紹介元種別、利用開始日、紹介料対象、紹介料支払状況、状態、メモを管理する。
- 利用者マスタに紹介者名がある場合、紹介者マスタと照合し、未登録なら `紹介者確認` として残す。

## 回数券・GMO入出金CSV

回数券管理は、`GMO入出金CSV貼付`、`入出金明細`、`訪問実績`、`利用者マスタ`、`回数券基準` をもとに更新する。

- `🎫 回数券を更新` は、貼り付け済みGMO入出金CSVを取り込み、回数券管理を更新する。
- 集計開始日は `2026/04/20`。
- 標準の回数券単価は `7,500円`。利用者マスタ側に単価があればそれを使う。
- `回数券基準` がある利用者は、基準日・基準残数以降で集計する。
- `回数券管理` では、回数券残数、当月利用回数、当月入金回数、未入金額、不足額、直近入金日などを確認する。

注意:

- GMO入出金CSVの貼付・取込は入金確認や請求判断に関わるため、実行前に対象CSVと対象期間を確認する。
- `GMO入出金CSV貼付` が空、またはシートがない場合は更新できない。
